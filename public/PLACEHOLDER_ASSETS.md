This folder holds static assets served at the site root.

Please add the following files before deploying:

1. `og-image.png` (1200x630) — used for social sharing previews (Open Graph / Twitter Card).
   Referenced in `app/layout.tsx`.

2. `Rehan-Ali-CV.pdf` — the downloadable CV linked from the "Download CV" button in the hero section.
   Referenced in `components/Hero.tsx`.

Once added, you can delete this file.
